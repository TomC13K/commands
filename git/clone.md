# Git clone

```bash
# clone the repo
git clone <repo> 
git clone <repo> <directory> # clone into specific dir
git clone --branch <tag> <repo> # clone specific tag
git clone --branch   # clone specific branch not the main/master
```